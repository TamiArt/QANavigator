
  # QA Navigator Web Application

  This is a code bundle for QA Navigator Web Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
