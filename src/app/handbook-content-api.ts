export const API_REST_SOAP_CONTENT = `## API: REST и SOAP

### Что такое API
API (Application Programming Interface) — интерфейс, через который программы обмениваются данными и вызывают функции друг друга. При API-тестировании QA проверяет бизнес-логику и интеграции напрямую через запросы и ответы, без обязательного участия графического интерфейса.

Для удалённого API базовая схема выглядит так:

\`клиент → request → сервер → response → клиент\`

В запросе тестировщик смотрит адрес, метод, заголовки, параметры и тело. В ответе — статус, заголовки, тело, структуру и корректность данных.

---

## REST API
REST (Representational State Transfer) — архитектурный стиль взаимодействия клиента и сервера. Обычно REST-сервисы работают через HTTP и передают данные в JSON, хотя формат не обязан ограничиваться JSON.

### Основные принципы REST
1. **Client-Server** — клиент и сервер разделены и могут развиваться независимо.
2. **Stateless** — каждый запрос содержит информацию, необходимую для его обработки; сервер не должен зависеть от сохранённого контекста предыдущего запроса.
3. **Cacheable** — ответы могут явно разрешать или запрещать кэширование.
4. **Uniform Interface** — ресурсы доступны через единообразный интерфейс.
5. **Layered System** — между клиентом и конечным сервером могут находиться промежуточные слои.
6. **Code on Demand** — необязательное ограничение: сервер может передавать клиенту исполняемый код.

### HTTP-методы
| Метод | Что делает | Повторный одинаковый запрос |
| --- | --- | --- |
| GET | Получает ресурс | Идемпотентный |
| POST | Создаёт ресурс или запускает операцию | Обычно неидемпотентный |
| PUT | Полностью заменяет состояние ресурса | Идемпотентный |
| PATCH | Частично изменяет ресурс | Зависит от реализации операции |
| DELETE | Удаляет ресурс | Идемпотентный по семантике метода |
| HEAD | Получает заголовки как у GET, без тела ответа | Идемпотентный |
| OPTIONS | Показывает доступные параметры взаимодействия | Идемпотентный |

**Идемпотентность** означает, что несколько одинаковых запросов должны иметь тот же ожидаемый эффект на состояние сервера, что и один такой запрос. Для QA это особенно важно при повторной отправке операций: например, повторный POST оплаты не должен случайно приводить к незапланированному двойному списанию, если бизнес-логика должна защищать от дублей.

### Часто встречающиеся HTTP-статусы
| Группа | Примеры | Смысл |
| --- | --- | --- |
| 2xx | 200 OK, 201 Created, 204 No Content | Запрос успешно обработан |
| 3xx | 301, 302, 304 | Перенаправление или работа с кэшем |
| 4xx | 400, 401, 403, 404, 405, 409, 422, 429 | Ошибка запроса, доступа или состояния ресурса |
| 5xx | 500, 502, 503, 504 | Ошибка на стороне сервера или его зависимостей |

Не проверяй статус отдельно от требований: конкретный API-контракт определяет, какой код ожидается в конкретном сценарии.

### Из чего состоит REST-запрос
\`\`\`text
Method: POST
Endpoint: /api/v1/users
Headers:
  Authorization: Bearer <token>
  Content-Type: application/json
Body:
  { "name": "Anna", "email": "anna@example.com" }
\`\`\`

### JSON: что важно QA
JSON хранит данные как объекты, массивы и значения. Проверяй не только наличие поля, но и его тип: строка, число, boolean, null, объект или массив. В JSON строки и имена ключей записываются в двойных кавычках; комментарии стандартный JSON не поддерживает.

---

## SOAP
SOAP (Simple Object Access Protocol) — протокол обмена структурированными сообщениями. SOAP-сообщения используют XML и следуют более строгому контракту, чем типичный REST API.

### SOAP-сообщение
Запрос помещается в **Envelope** — SOAP-конверт. Внутри него находятся:
- **Header** — необязательная служебная информация;
- **Body** — данные и вызываемая операция;
- **Fault** — структура ошибки, которая передаётся в Body при SOAP-ошибке.

Упрощённая структура:
\`\`\`xml
<Envelope>
  <Header>...</Header>
  <Body>
    <Operation>...</Operation>
  </Body>
</Envelope>
\`\`\`

### XML: что важно QA
XML — язык разметки для структурированных данных. При проверке XML обращай внимание на:
- один корневой элемент;
- закрывающие теги и правильную вложенность;
- регистр имён тегов;
- кавычки у значений атрибутов;
- корректные специальные символы;
- namespaces, если они заданы контрактом.

Синтаксически неверный XML не должен рассматриваться как корректный запрос.

### WSDL — контракт SOAP-сервиса
WSDL (Web Services Description Language) описывает интерфейс SOAP-сервиса: доступные операции, сообщения, типы данных, способ передачи и адрес сервиса.

Основные части WSDL:
| Элемент | Что описывает |
| --- | --- |
| definitions | Корневое описание сервиса |
| types | Используемые типы данных |
| message | Структуру сообщений |
| portType | Доступные операции |
| binding | Как операции связываются с протоколом передачи |
| service | Точку доступа к сервису |

Для тестировщика WSDL — источник контракта. Его можно импортировать в SoapUI или поддерживающий импорт инструмент, чтобы получить шаблоны запросов и сверять фактическое поведение с описанием сервиса.

### XSD — схема XML
XSD (XML Schema Definition) описывает структуру XML-данных: элементы, типы, обязательность и ограничения. Связь удобно запомнить так:

\`WSDL → какие операции и сообщения есть у сервиса\`

\`XSD → как устроены данные этих сообщений\`

\`XML → конкретные данные запроса или ответа\`

WSDL может ссылаться на XSD. Для QA важно проверять, соответствует ли XML фактической схеме, и не разошлись ли реализация, WSDL и XSD после изменений.

---

## REST и SOAP: главное различие
| Характеристика | REST | SOAP |
| --- | --- | --- |
| Природа | Архитектурный стиль | Протокол сообщений |
| Данные | Часто JSON, возможны и другие представления | SOAP-сообщение в XML |
| Контракт | Часто OpenAPI / Swagger | WSDL, часто вместе с XSD |
| Основной объект проверки | HTTP-взаимодействие, ресурсы, данные | XML-сообщение и соответствие SOAP-контракту |
| Ошибки | HTTP-статус + тело ответа | HTTP-уровень + SOAP Fault |
| Инструменты из материалов | Postman, DevTools, Swagger/OpenAPI | SoapUI, WSDL; также возможна работа через Postman |

SOAP не нужно тестировать как REST. Для REST основной ориентир обычно HTTP-контракт и структура ответа; для SOAP дополнительно критичны структура XML, WSDL/XSD и Fault.

---

## План тестирования API для QA

### 1. Сначала изучи контракт
Для REST изучи доступную документацию: endpoint, метод, параметры, headers, body, авторизацию и ожидаемые ответы. Для SOAP начни с WSDL и связанных XSD. Не придумывай обязательность, лимиты и коды ответа, если их нет в требованиях.

### 2. Проверь позитивный сценарий
Отправь корректный запрос с валидными данными.

Проверь:
- операция выполняется по требованиям;
- возвращается ожидаемый статус или SOAP-ответ;
- структура тела соответствует контракту;
- значения и типы данных корректны;
- изменение действительно произошло в системе, если запрос меняет данные.

### 3. Проверь входные данные
Для каждого параметра или поля используй известные ограничения:
- обязательное поле отсутствует;
- пустая строка и null, если применимо;
- неверный тип данных;
- значение меньше/больше известной границы;
- строка короче/длиннее известного лимита;
- неверный формат;
- неожиданные специальные символы.

Ожидаемое поведение определяется требованиями. Некорректный ввод не должен приводить к необработанному падению сервиса.

### 4. Проверь авторизацию и права
Если endpoint защищён:
- запрос без учётных данных;
- неверные или повреждённые учётные данные;
- истёкший токен, если у него есть срок жизни;
- валидный пользователь без требуемой роли;
- попытка обратиться к чужому ресурсу под своей учётной записью.

Различай аутентификацию и авторизацию: валидный пользователь может быть успешно распознан, но не иметь права на конкретную операцию.

### 5. Проверь повторные запросы и состояние
Для REST учитывай семантику метода и идемпотентность. Проверяй повторную отправку, повторное удаление, обновление уже изменённого ресурса и дублирующие операции. Для денежных и других критичных операций отдельно проверяй защиту от непреднамеренного повторного выполнения.

### 6. Проверь ошибки
Для REST сверяй HTTP-статус и тело ошибки с контрактом. Не ограничивайся проверкой только 200 OK.

Для SOAP дополнительно проверяй **Fault**: ошибка должна быть обработана и возвращена в предусмотренной сервисом форме. Проверь реакцию на XML, который нарушает ожидаемую структуру или схему.

### 7. Проверь контракт SOAP
Для SOAP отдельно проверь:
- XML синтаксически корректен;
- Envelope и Body имеют ожидаемую структуру;
- Header присутствует, если он требуется;
- операция существует в WSDL;
- обязательные элементы переданы;
- типы и ограничения соответствуют XSD;
- namespaces соответствуют контракту;
- ответ соответствует WSDL/XSD;
- ошибки возвращаются через предусмотренный Fault;
- WSDL/XSD не расходятся с фактическим сервисом.

### 8. Проверь заголовки и формат
Сверяй Content-Type, Authorization и другие обязательные заголовки с контрактом. Для REST проверяй фактический формат ответа. Для SOAP — ожидаемый XML и настройки, заданные сервисом.

### 9. Проверь интеграцию
Если API связывает несколько систем, проверяй не только факт ответа. Убедись, что данные передались дальше, сохранились или изменились там, где это предусмотрено сценарием. Ошибка одной системы должна корректно обрабатываться другой.

### 10. После изменений проведи регрессию
Повтори критические позитивные и негативные сценарии изменённого API и связанных интеграций. Особое внимание — контрактам: изменение структуры ответа, WSDL или XSD может сломать клиентов даже при работающей бизнес-логике.

---

## Короткий чек-лист
\`\`\`text
[ ] Контракт изучен, неизвестные требования не придуманы
[ ] Позитивный запрос работает
[ ] Обязательные поля и типы проверены
[ ] Границы и форматы проверены, если они известны
[ ] Негативные данные обработаны корректно
[ ] Авторизация и роли проверены
[ ] Повторные запросы не создают неожиданный эффект
[ ] Статус и тело ответа соответствуют требованиям
[ ] Ошибки обработаны, сервер не падает необработанно
[ ] Для SOAP проверены XML, WSDL, XSD, namespaces и Fault
[ ] Интеграции сохраняют и передают корректные данные
[ ] После изменений выполнена регрессия
\`\`\`

### Инструменты из учебных материалов
- **Postman / Insomnia** — отправка и организация API-запросов.
- **Swagger / OpenAPI** — описание и интерактивная документация REST API.
- **SoapUI** — работа с SOAP и WSDL.
- **DevTools → Network** — просмотр запросов, отправляемых фронтендом.
- **Charles Proxy / Fiddler** — перехват и анализ трафика.
- **curl** — отправка запросов из командной строки.

Главный принцип для тестировщика: проверяй не только то, что запрос получил ответ, а весь контракт — входные данные, права, бизнес-логику, структуру ответа, ошибки и фактическое изменение состояния системы.`;
